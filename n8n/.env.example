# Copia a .env y ajusta si quieres (opcional; el compose tiene valores por defecto).
# Zona horaria para triggers programados y fechas en n8n (IANA, ej. Europe/Madrid).
GENERIC_TIMEZONE=Europe/Madrid
TZ=Europe/Madrid

